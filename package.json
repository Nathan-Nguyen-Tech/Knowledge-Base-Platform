{
  "name": "knowledge-base-platform",
  "version": "1.0.0",
  "description": "Multi-department Knowledge Base Platform with MCP servers",
  "private": true,
  "workspaces": [
    "packages/*",
    "departments/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "keywords": [
    "mcp",
    "knowledge-base",
    "onedrive",
    "multi-department"
  ],
  "author": "Compass",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
